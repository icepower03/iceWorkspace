// Type definitions for jquery.alphanum.js
// Project: 
// Definitions by: LC
/*

interface iOptionNum
{
    allowPlus?: boolean, // Allow the + sign
    allowMinus?: boolean,  // Allow the - sign
    allowThouSep?: boolean,  // Allow the thousands separator, default is the comma eg 12,000
    allowDecSep?: boolean,  // Allow the decimal separator, default is the fullstop eg 3.141
    allowLeadingSpaces?: boolean,
    maxDigits?: string,    // The max number of digits or '' for no m 
    maxDecimalPlaces?: string,    // The max number of decimal places or '' for no m 
    maxPreDecimalPlaces?: string,     // The max number digits before the decimal point or '' for no m 
    DEC_SEP?: string,
    max?: string,    // The max numeric value allowed
    min?: string

}


interface JQuery {
    numeric(settings: iOptionNum): JQuery;
}
*/