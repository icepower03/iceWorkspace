import { OptionsHtml } from '../iceBase';
import { iceElement } from '../../iceElement';

export class iceTable extends iceElement {
    constructor(options?: OptionsHtml) {
        super("table", options);
    }
}
